Genesis PRD를 기반으로 시스템 전역 에러 통신 규격 및 공통 페이로드 구조를 정의하십시오.

## [SAD - Global Context 설계 원칙]
1. **SSOT 상속**: 주입된 `Genesis PRD`를 절대적인 진실 공급원(SSOT)으로 삼으십시오.
2. **프로토콜 종속성**: `Genesis PRD`의 `api_type`(REST, GraphQL, GRPC)에 맞는 표준 규격을 도출하십시오.
3. **인프라 및 보안 정합성**: `$PREVIOUS_ARCHITECTURAL_DECISIONS`의 `sad_tech_stack`(선택한 프레임워크의 에러 처리 관례) 및 `sad_auth_rbac`(인증 실패 대응 규격)과 일치하는 인터페이스 표준을 수립하십시오.
4. **이전 설계 계승(Refinement)**: `$PREVIOUS_DRAFT`에 데이터가 존재한다면 이를 기본 골격으로 삼고, `$EVALUATOR_FEEDBACK`의 교정 지시 사항을 최우선으로 반영하여 에러 코드 및 응답 구조를 개선하십시오.

---

## [에러 및 통신 규격 규칙]

1. **전역 에러 객체 표준 (Error Standard)**:
   - `api_type`의 특성에 맞는 전역 에러 응답 객체의 표준 JSON 구조(Payload Schema)를 정의하십시오.
   - 시스템 공통 예외(인증 오류, 권한 없음, 데이터 없음, 서버 내부 오류 등)에 대한 표준 코드를 매핑하십시오.
   - 각 에러 코드와 이에 대응하는 HTTP Status Code(또는 프로토콜 표준 코드) 매핑 기준을 명시하십시오.

2. **일관성 강제**:
   - 도메인별 예외가 아닌, 시스템 전체에서 재사용될 수 있는 범용적인 에러 상황에 집중하십시오.

3. **마이크로 설계 배제**: 개별 비즈니스 API의 엔드포인트 경로, 특정 API 전용 요청/응답 필드 등 마이크로 명세는 다루지 마십시오.

[출력 형식 가이드]
```json
{
  "api_protocol": "REST / GraphQL / gRPC 등",
  "common_header": {
    "key": "value",
    "description": "..."
  },
  "common_response": {
    "success_format": "...",
    "error_format": "..."
  },
  "error_codes": [
    {
      "code": "ERR_CODE_XXX",
      "message": "...",
      "http_status": 400
    }
  ]
}
```

반드시 마크다운 백틱(```json)이나 인사말 없이, 순수하고 유효한 JSON 객체 1개만 출력하십시오.
인사말이나 부연 설명을 절대 붙이지 마십시오.
