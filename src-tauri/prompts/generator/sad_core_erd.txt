Genesis PRD를 기반으로 전체 시스템의 데이터베이스 표준 및 코어 엔티티 설계를 작성하십시오.

## [SAD - Global Context 설계 원칙]
1. **SSOT 상속**: 주입된 `Genesis PRD`를 절대적인 진실 공급원(SSOT)으로 삼으십시오.
2. **전역 표준 중심**: 개별 비즈니스 로직에 종속된 마이크로 설계가 아닌, 시스템 전체를 관통하는 공통 규격에 집중하십시오.
3. **기술 스택 정합성**: `$PREVIOUS_ARCHITECTURAL_DECISIONS`에 주입된 `sad_tech_stack`에서 확정된 데이터베이스 종류 및 사양에 최적화된 엔티티 설계를 수행하십시오.
4. **이전 설계 계승(Refinement)**: `$PREVIOUS_DRAFT`에 데이터가 존재한다면 이를 기본 골격으로 삼고, `$EVALUATOR_FEEDBACK`의 교정 지시 사항을 최우선으로 반영하여 엔티티 및 관계를 점진적으로 개선하십시오.
4. **마이크로 설계 배제 (절대 금지)**:
   - 개별 물리 테이블의 상세 컬럼 사양(예: `VARCHAR(255)`, `INT`, `PRIMARY KEY`, `NOT NULL` 등)을 절대 기술하지 마십시오.
   - 데이터 타입을 명시하거나 제약 조건을 나열하는 행위는 '마이크로 설계 침범'으로 간주되어 평가에서 감회됩니다.
   - 전역 컨텍스트에서는 오직 '비즈니스 개념'과 '엔티티 간의 관계'에만 집중하십시오.

---

## [ERD 표준 및 코어 엔티티 규칙]

1. **명명 규칙 (Naming Convention)**:
   - 전주 명명 규칙을 Enum(예: `snake_case`, `camelCase`, `PascalCase`) 중 하나로 확정하여 기술하십시오.
   - 테이블명 및 컬럼명은 확정된 규칙에 따라 통일하십시오 (boolean 필드는 `is_`, `has_` 접두어 권장).

2. **전역 메타 표준 (Global Meta Standard - 필수)**:
   - 시스템 내 모든 테이블이 필수적으로 상속받아야 할 메타 컬럼(예: `created_at`, `updated_at`, `is_deleted`)의 명칭과 속성을 정의하십시오.
   - 아래 `output_format`의 `global_meta_info` 섹션에 이를 반드시 명시하십시오.

3. **코어 엔티티 도출 및 속성 추상화**:
   - `Genesis PRD`의 `core_epics`를 기반으로 시스템의 핵심 엔티티를 식별하십시오.
   - **속성 제한**: 각 엔티티당 비즈니스 속성 3~5개 내외만 기술하십시오.
   - **금지 사항**: 필드명 뒤에 데이터 타입이나 DB 제약 조건을 붙이지 마십시오. (예: `user_id (UUID)` -> `user_id`로만 작성)
   - 세부 컬럼 명세보다는 엔티티 간의 관계와 데이터 흐름의 정합성에 집중하십시오.

[출력 형식 가이드]
```json
{
  "global_meta_info": {
    "naming_convention": "snake_case",
    "common_columns": ["id", "created_at", "updated_at", "is_deleted"],
    "description": "전체 시스템에 공통 적용될 데이터 표준 정의"
  },
  "core_entities": [
    {
      "entity_name": "...",
      "attributes": ["속성명1", "속성명2", "속성명3"],
      "description": "..."
    }
  ],
  "relationships": [
    {
      "from": "Entity A",
      "to": "Entity B",
      "type": "1:N",
      "description": "..."
    }
  ]
}
```

반드시 마크다운 백틱(```json)이나 인사말 없이, 순수하고 유효한 JSON 객체 1개만 출력하십시오.
인사말이나 부연 설명을 절대 붙이지 마십시오.
